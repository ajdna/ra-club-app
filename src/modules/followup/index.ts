/**
 * followup module
 *
 * Follow-up Engine. Auto-generates the 90-day Consumer Follow-Up task list (3 x 30-day cycles of calls / home visits / reminders) per coach per member each day.
 *
 * See src/modules/README.md for the full module map, and the Technical
 * Architecture doc (Section 3) for detailed responsibilities.
 */

export {};

