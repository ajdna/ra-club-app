/**
 * health-score module
 *
 * Health Score & Early Warning. Daily 5-signal composite (attendance, response, engagement, weight-log, lifecycle stage) -> Green/Yellow/Red with intervention playbooks. Includes the silent Coach Wellness sub-job.
 *
 * See src/modules/README.md for the full module map, and the Technical
 * Architecture doc (Section 3) for detailed responsibilities.
 */

export {};

