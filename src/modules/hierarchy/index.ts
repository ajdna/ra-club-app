/**
 * hierarchy module
 *
 * Hierarchy & Tree. The org tree with unlimited depth, implemented as a closure table. Answers 'show my whole downline' and 'can user A see member B?' as single indexed queries. Enforces sideline isolation.
 *
 * See src/modules/README.md for the full module map, and the Technical
 * Architecture doc (Section 3) for detailed responsibilities.
 */

export {};

