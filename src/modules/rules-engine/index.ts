/**
 * rules-engine module
 *
 * Rules Engine. The configurability core. Stores pricing, qualification criteria, scoring weights, cadences, gift catalog, and notification templates as versioned config. Edited via the Admin Console; changes propagate instantly, no deploy.
 *
 * See src/modules/README.md for the full module map, and the Technical
 * Architecture doc (Section 3) for detailed responsibilities.
 */

export {};

