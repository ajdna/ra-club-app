/**
 * identity module
 *
 * Identity & Roles. User accounts, phone/OTP auth profiles, and role assignment (Club Owner, NCO, JCO, Coach, Member, Privilege, Guest, Upline). Backs RBAC enforced at the API layer.
 *
 * See src/modules/README.md for the full module map, and the Technical
 * Architecture doc (Section 3) for detailed responsibilities.
 */

export {};

